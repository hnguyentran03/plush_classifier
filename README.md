# Plushie Classifier
## Data Scraper
To get the data needed, `get_images.py` scrapes the internet for various types of plushies. First, download the dependencies via 
```bash
pip install -r requirements.txt
```
Then to scarpe the images, navigate to the main folder directory and run
```bash
python src/get_images.py
```

## Classifier

TODO